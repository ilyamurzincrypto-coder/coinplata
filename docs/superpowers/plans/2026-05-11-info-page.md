# «Справка / Info» Page Implementation Plan (Spec C.5-info)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a new top-level «Справка» page — a plain-Russian manual of every feature in the service (what it is, what it's connected to, what it can do), reachable from the header nav.

**Architecture:** Pure static content in `src/pages/info/content.js` (`INFO_SECTIONS` array); a thin `src/pages/InfoPage.jsx` renders it as a collapsible accordion (`InfoCard`). Wired in via `Header.jsx` (`NAV_PAGES` += `info`), `App.jsx` (page render), and one i18n key `nav_info`. No store, no permission gating, no DB.

**Tech Stack:** Vite + React 18 + Tailwind 3; Vitest + @testing-library/react.

**Spec:** `docs/superpowers/specs/2026-05-11-info-page-design.md`.

---

## Phase 0 — Branch + baseline

### Task 0.1
- [ ] `git branch --show-current` → `feat/info-page`.
- [ ] `npx vitest run --no-file-parallelism` → all green (34 files / 321 tests as of the subconto merge). Note counts.
- [ ] `npm run build` → succeeds.

---

## Task 1: Content module `src/pages/info/content.js` (+ sanity test)

**Files:** Create `src/pages/info/content.js`. Test: `src/pages/info/content.test.js`.

`INFO_SECTIONS` is an array of `{ id, title, what, related, can: [...], sub?: [{ id, title, what?, related?, can: [...] }] }`. Prose is plain Russian. The skeleton + gist for each section is in the spec's "Content module" section — expand it sensibly into short, jargon-free sentences; do NOT invent features that don't exist (cross-check against the repo's `CLAUDE.md` and the `docs/superpowers/` specs).

- [ ] **Step 1: Failing test** — create `src/pages/info/content.test.js`:

```js
import { describe, it, expect } from "vitest";
import { INFO_SECTIONS } from "./content.js";

const nonEmptyStr = (v) => typeof v === "string" && v.trim().length > 0;

describe("INFO_SECTIONS", () => {
  it("is a non-empty array with unique ids and well-formed sections", () => {
    expect(Array.isArray(INFO_SECTIONS)).toBe(true);
    expect(INFO_SECTIONS.length).toBeGreaterThanOrEqual(6);
    const ids = INFO_SECTIONS.map((s) => s.id);
    expect(new Set(ids).size).toBe(ids.length); // unique
    for (const s of INFO_SECTIONS) {
      expect(nonEmptyStr(s.id)).toBe(true);
      expect(nonEmptyStr(s.title)).toBe(true);
      expect(nonEmptyStr(s.what)).toBe(true);
      expect(nonEmptyStr(s.related)).toBe(true);
      expect(Array.isArray(s.can) && s.can.length > 0 && s.can.every(nonEmptyStr)).toBe(true);
      if (s.sub) {
        expect(Array.isArray(s.sub) && s.sub.length > 0).toBe(true);
        for (const ss of s.sub) {
          expect(nonEmptyStr(ss.title)).toBe(true);
          expect(Array.isArray(ss.can) && ss.can.length > 0 && ss.can.every(nonEmptyStr)).toBe(true);
        }
      }
    }
  });
  it("covers the core areas", () => {
    const ids = INFO_SECTIONS.map((s) => s.id);
    for (const id of ["cashier", "capital", "accounts", "counterparties", "treasury", "settings"]) expect(ids).toContain(id);
    const treasury = INFO_SECTIONS.find((s) => s.id === "treasury");
    expect(treasury.sub.map((s) => s.id)).toEqual(expect.arrayContaining(["balance-sheet", "pnl", "turnover", "journal", "posting-master"]));
  });
});
```

- [ ] **Step 2: Run, expect FAIL** — `npx vitest run src/pages/info/content.test.js` → FAIL (cannot resolve `./content.js`).

- [ ] **Step 3: Implement `content.js`** — create `src/pages/info/content.js`. Skeleton (fill the `…` with plain-Russian prose per the spec — keep sentences short; the bullets below are illustrative, expand to 3–7 per area):

```js
// src/pages/info/content.js
// Plain-Russian manual of the service's features, rendered by InfoPage.
// Each section: { id, title, what (one sentence), related (one sentence), can (bullets), sub? }.

export const INFO_SECTIONS = [
  {
    id: "cashier", title: "Касса",
    what: "Главный экран оператора: текущие курсы и балансы счетов плюс создание сделок.",
    related: "Держится на счетах (движения денег), курсах (каналы и пары), клиентах и обязательствах (отложенные выплаты).",
    can: [
      "Создать обмен с несколькими ногами «пришло» и «ушло» (можно сразу несколько валют).",
      "Принять средства от клиента на наш счёт (top-up).",
      "Сделать перевод между нашими счетами.",
      "Записать доход или расход.",
      "Отредактировать уже созданную сделку.",
      "Видеть открытые обязательства (что мы ещё должны выдать) и закрывать их.",
    ],
  },
  {
    id: "capital", title: "Капитал",
    what: "Сводки по всему капиталу компании, пересчитанные в базовую валюту.",
    related: "Берёт балансы всех счетов, курсы (для пересчёта в базовую валюту), рефералов и клиентов.",
    can: [
      "Показать, сколько всего денег и где они лежат.",
      "Показать прибыль.",
      "Реферальные начисления.",
      "LTV (сколько принёс каждый клиент).",
    ],
  },
  {
    id: "accounts", title: "Счета",
    what: "Список всех наших счетов: касса, банк, крипто-кошельки.",
    related: "Балансы считаются из движений (нигде не хранятся как число). Привязаны к офисам.",
    can: [
      "Видеть по каждому счёту: баланс, сколько зарезервировано, сколько доступно.",
      "Историю движений по счёту.",
      "Добавить счёт; импорт и экспорт списка.",
    ],
  },
  {
    id: "counterparties", title: "Контрагенты",
    what: "Клиенты и OTC-партнёры в одном разделе.",
    related: "Связаны со сделками, обязательствами и рефералами.",
    can: [
      "Список клиентов и партнёров.",
      "Профиль контрагента с историей операций.",
      "Вкладка обязательств: кто кому сколько должен.",
    ],
  },
  {
    id: "treasury", title: "Казначейство",
    what: "Бухгалтерский раздел на двойной записи: транзакции, журнальные проводки Дт/Кт и план счетов (174 счёта).",
    related: "Сюда попадает всё, что пишется в v2-леджер: сделки кассы, переводы, пополнения, корректировки, ручные проводки. Для базовой валюты использует курсы.",
    can: ["Смотреть баланс, P&L, обороты, журнал проводок и вводить ручные проводки — подробнее в подпунктах ниже."],
    sub: [
      { id: "balance-sheet", title: "Активы / Пассивы / Капитал",
        what: "Баланс по классам счетов на сейчас.",
        can: [
          "Видеть остаток по каждому счёту, сгруппированный по типу.",
          "Развернуть счёт → его проводки за всё время.",
          "Для счетов с субконто (обязательства перед клиентами/партнёрами) — развернуть до строк по каждому клиенту/партнёру, а их — до проводок этого контрагента.",
          "Внизу прилипшая проверка: Активы = Пассивы + Капитал (зелёная галочка или красная дельта).",
        ] },
      { id: "pnl", title: "P&L (прибыли и убытки)",
        what: "Отчёт о прибыли за выбранный период.",
        can: [
          "Доходы, расходы, курсовые разницы, чистая прибыль.",
          "Пресеты периода: сегодня / неделя / месяц / квартал / год / 30 дней.",
          "Экспорт в CSV.",
          "Тумблер «сравнить с прошлым периодом» — добавляет колонки «прошлый период» и «Δ» к каждой строке.",
        ] },
      { id: "turnover", title: "Обороты",
        what: "Оборотно-сальдовая ведомость и шахматка за период.",
        can: [
          "ОСВ: по каждому счёту — остаток на начало, оборот по дебету, оборот по кредиту, остаток на конец; с проверками тождеств и экспортом CSV.",
          "Шахматка: матрица оборотов «счёт по дебету × счёт по кредиту» за период.",
        ] },
      { id: "journal", title: "Журнал",
        what: "Хронология всех транзакций леджера.",
        can: [
          "Развернуть транзакцию → таблица проводок Дт/Кт.",
          "Перейти в исходный документ (сделку и т.п.).",
          "Фильтр по типу: сделки / переводы / пополнения / корректировки / ручные / сторно.",
          "У ручных проводок — кнопка «Сторнировать».",
        ] },
      { id: "posting-master", title: "Ручная проводка",
        what: "Конструктор ручной проводки с несколькими плечами — для корректировок, реклассификаций, начислений, ручных комиссий.",
        related: "Пишется в леджер и в audit-trail; доступно только ролям owner и accountant.",
        can: [
          "Выбрать счета, ввести суммы по дебету/кредиту; проводка должна сходиться (Σ Дт = Σ Кт), одна валюта.",
          "Предпросмотр перед проведением; после — сразу видна в Журнале (фильтр «ручные»).",
          "Сторнировать ранее проведённую ручную проводку из Журнала.",
          "Счета с обязательным субконто (клиенты/партнёры) пока в этом конструкторе недоступны.",
        ] },
    ],
  },
  {
    id: "settings", title: "Настройки",
    what: "Администрирование сервиса.",
    related: "Пользователи, права, офисы, курсы, базовая валюта.",
    can: [
      "Пользователи и приглашения.",
      "Матрица прав: роль × раздел × уровень (выключено / просмотр / редактирование).",
      "Офисы.",
      "Курсы: каналы (наличные/банк/SEPA/SWIFT для фиата; сеть с gas-fee для крипты), пары, статус draft → confirmed.",
      "Базовая валюта (в ней считаются все сводные метрики).",
    ],
  },
  {
    id: "audit", title: "Аудит",
    what: "Журнал действий пользователей.",
    related: "Фиксирует операции во всех разделах.",
    can: ["Видеть, кто что и когда сделал."],
  },
  {
    id: "glossary", title: "Как всё связано (глоссарий)",
    what: "Короткие пояснения ключевых понятий, чтобы остальные разделы были понятнее.",
    related: "Это не функция, а справка по терминам.",
    can: [
      "Движения (movements) — атомарные перемещения денег; балансы счетов считаются из них и нигде не хранятся отдельным числом.",
      "v2-леджер — двойная запись: каждая транзакция = пары проводок Дт/Кт по плану счетов. Раздел «Казначейство» показывает именно его.",
      "Субконто — измерение на счёте-обязательстве: на счёте «обязательства перед клиентами» хранится отдельный остаток по каждому клиенту.",
      "Базовая валюта — валюта, в которой считаются сводные метрики (Капитал, P&L и т.п.); исходные данные остаются в своих валютах, пересчёт — на момент показа.",
      "Фича-флаги VITE_USE_NEW_DEAL_FORM (новая форма сделки) и VITE_USE_NEW_LEDGER (запись операций через ledger.* вместо легаси-таблиц) — включают v2-флоу; откат — выключить флаг и передеплоить.",
    ],
  },
];
```

  (Expand the `can` bullets where they're thin — but keep every bullet a short, plain sentence. Cross-check against `CLAUDE.md` so nothing is invented. The two `content.test.js` assertions enforce the structure and the core-area coverage; don't drop sections those tests require.)

- [ ] **Step 4: Run, expect PASS** — `npx vitest run src/pages/info/content.test.js` → all green.
- [ ] **Step 5: Commit:**
```bash
git add src/pages/info/content.js src/pages/info/content.test.js
git commit -m "feat(info): INFO_SECTIONS content module — plain-RU feature manual"
git push
```

---

## Task 2: `src/pages/InfoPage.jsx` (+ render test)

**Files:** Create `src/pages/InfoPage.jsx`. Test: `src/pages/InfoPage.test.jsx`.

- [ ] **Step 1: Failing test** — create `src/pages/InfoPage.test.jsx`:

```jsx
import React from "react";
import { describe, it, expect, vi } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";

vi.mock("../i18n/translations.jsx", () => ({ useTranslation: () => ({ t: (k) => k }) }));

import InfoPage from "./InfoPage.jsx";
import { INFO_SECTIONS } from "./info/content.js";

describe("InfoPage", () => {
  it("renders every section's title", () => {
    render(<InfoPage />);
    for (const s of INFO_SECTIONS) expect(screen.getByText(s.title)).toBeInTheDocument();
  });
  it("expanding a section reveals its 'what' line and bullets", () => {
    render(<InfoPage />);
    const first = INFO_SECTIONS[0];
    // click the section header (its title) to expand
    fireEvent.click(screen.getByText(first.title));
    expect(screen.getByText(first.what)).toBeInTheDocument();
    expect(screen.getByText(first.can[0])).toBeInTheDocument();
  });
});
```

  (If you choose to render the first card open by default, adjust the second test — e.g. click the *second* section, or assert without clicking the first. Keep the assertion's intent: expanding shows `what` + `can[0]`.)

- [ ] **Step 2: Run, expect FAIL** — `npx vitest run src/pages/InfoPage.test.jsx` → FAIL (cannot resolve `./InfoPage.jsx`).

- [ ] **Step 3: Implement `InfoPage.jsx`:**

```jsx
// src/pages/InfoPage.jsx
// Справка / Info — a plain-language manual of every feature in the service.
// Pure render of INFO_SECTIONS as a collapsible accordion. No store, no permissions.
import React, { useState } from "react";
import { ChevronRight, ChevronDown } from "lucide-react";
import { INFO_SECTIONS } from "./info/content.js";

function Bullets({ items }) {
  return (
    <ul className="mt-1.5 space-y-1 text-[12.5px] text-slate-700 list-disc pl-5">
      {items.map((b, i) => <li key={i}>{b}</li>)}
    </ul>
  );
}

function SubCard({ sub }) {
  return (
    <div className="rounded-[10px] border border-slate-100 bg-slate-50/50 px-3 py-2">
      <div className="text-[12.5px] font-bold text-slate-900">{sub.title}</div>
      {sub.what && <div className="text-[12px] text-slate-500 italic mt-0.5">{sub.what}</div>}
      {sub.related && <div className="text-[12px] text-slate-500 mt-0.5"><span className="font-medium text-slate-600">Связано: </span>{sub.related}</div>}
      <Bullets items={sub.can} />
    </div>
  );
}

function InfoCard({ section, defaultOpen }) {
  const [open, setOpen] = useState(!!defaultOpen);
  return (
    <section className="bg-white rounded-[14px] border border-slate-200/70 overflow-hidden">
      <header className="px-4 py-3 flex items-center gap-2 cursor-pointer hover:bg-slate-50" onClick={() => setOpen((v) => !v)}>
        {open ? <ChevronDown className="w-4 h-4 text-slate-400 shrink-0" /> : <ChevronRight className="w-4 h-4 text-slate-400 shrink-0" />}
        <h2 className="text-[14px] font-bold text-slate-900">{section.title}</h2>
      </header>
      {open && (
        <div className="px-4 pb-4 pt-1 space-y-2">
          <p className="text-[12.5px] text-slate-500 italic">{section.what}</p>
          <p className="text-[12.5px] text-slate-600"><span className="font-medium text-slate-700">С чем связано: </span>{section.related}</p>
          <div className="text-[12px] font-medium text-slate-700">Что умеет:</div>
          <Bullets items={section.can} />
          {section.sub && (
            <div className="mt-2 space-y-2">
              {section.sub.map((ss) => <SubCard key={ss.id || ss.title} sub={ss} />)}
            </div>
          )}
        </div>
      )}
    </section>
  );
}

export default function InfoPage() {
  return (
    <main className="max-w-[900px] mx-auto px-6 py-6 space-y-3">
      <header className="space-y-1">
        <h1 className="text-[22px] font-bold tracking-tight">Справка</h1>
        <p className="text-[13px] text-slate-500">Что есть в сервисе и что оно умеет — простым языком. Разверни любой раздел.</p>
      </header>
      {INFO_SECTIONS.map((s, i) => <InfoCard key={s.id} section={s} defaultOpen={i === 0} />)}
    </main>
  );
}
```

- [ ] **Step 4: Run, expect PASS + build** — `npx vitest run src/pages/InfoPage.test.jsx` then `npm run build` → tests green (adjust the second test per the `defaultOpen` note if needed — the first card is open by default in the code above, so the second test should click `INFO_SECTIONS[1].title` and assert `INFO_SECTIONS[1].what` instead; OR just keep the test as-is and remove `defaultOpen={i === 0}`. Pick one and make the test pass without changing its intent); build clean.
- [ ] **Step 5: Commit:**
```bash
git add src/pages/InfoPage.jsx src/pages/InfoPage.test.jsx
git commit -m "feat(info): InfoPage — collapsible feature-manual accordion"
git push
```

---

## Task 3: Wire into nav (`nav_info` i18n + `Header.jsx` + `App.jsx`)

**Files:** Modify `src/i18n/translations.jsx`, `src/components/Header.jsx`, `src/App.jsx`.

- [ ] **Step 1: i18n `nav_info`** — in `src/i18n/translations.jsx`, find the `nav_*` keys in each locale block (`nav_cashier`, `nav_treasury`, …). Right after `nav_settings` (or `nav_treasury`) add: EN `nav_info: "Info",` — RU `nav_info: "Справка",` — TR `nav_info: "Bilgi",`.

- [ ] **Step 2: `Header.jsx`** — find the `NAV_PAGES` array (`[{ id: "cashier", key: "nav_cashier", section: "transactions" }, …]`). Add at the end:

```jsx
  { id: "info", key: "nav_info", section: "transactions" },
```

(`section: "transactions"` makes it visible to all defined roles. No other change — the existing `NAV_PAGES.filter((p) => can(p.section))` handles visibility, and the existing `.map` renders the button.)

- [ ] **Step 3: `App.jsx`** — add `import InfoPage from "./pages/InfoPage.jsx";` near the other page imports, and add the render alongside the others (e.g. after the `settings` line):

```jsx
      {page === "info" && canShow("info") && <InfoPage />}
```

(`PAGE_SECTION` is **not** modified — `canShow(p) = can(PAGE_SECTION[p] || "transactions")`, so `canShow("info")` falls back to `can("transactions")` which is truthy for all roles. The `page` state can already become `"info"` via `handlePageChange` since `Header` will emit it.)

- [ ] **Step 4: Build** — `npm run build` → succeeds.
- [ ] **Step 5: Commit:**
```bash
git add src/i18n/translations.jsx src/components/Header.jsx src/App.jsx
git commit -m "feat(info): nav entry + page route for the Справка page"
git push
```

---

## Phase 7 — Final + PR

### Task 7.1
- [ ] `npx vitest run --no-file-parallelism` → all green. New since baseline: content sanity ~2, InfoPage render ~2.
- [ ] `npm run build` → clean.
- [ ] **Local smoke (manual — note in PR if skipped):** `npm run dev` → a «Справка» item in the header nav (for any role); the page shows an accordion of areas; expanding one shows «Что это / С чем связано / Что умеет»; «Казначейство» has the sub-cards.
- [ ] **Open PR:**
```bash
gh pr create --base main --head feat/info-page --title "feat(info): Справка page — plain-language feature manual (Spec C.5-info)" --body "$(cat <<'EOF'
## Summary
New top-level «Справка» page (header nav, visible to everyone) — a plain-Russian manual of every feature in the service: for each area, «Что это / С чем связано / Что умеет»; Казначейство has sub-cards for its tabs; a final «Как всё связано» glossary block (movements → balances, v2 ledger → Treasury, субконто, base currency, the VITE_* flags).

Pure static content: `src/pages/info/content.js` (`INFO_SECTIONS`) + `src/pages/InfoPage.jsx` (collapsible accordion). Wired via `Header.jsx` (NAV_PAGES += info), `App.jsx` (page route), one i18n key `nav_info`. No store, no permission gating, no DB.

## Test plan
- [x] Full suite green (new: content sanity, InfoPage render)
- [x] `npm run build` clean
- [ ] Local smoke: «Справка» in the nav for any role; accordion expands; Казначейство sub-cards present

## Out of scope
Search within the manual; screenshots/video; versioned changelog; en/tr translation of the manual (RU only — only `nav_info` is i18n'd); deep-links from manual entries to the pages.

Spec: `docs/superpowers/specs/2026-05-11-info-page-design.md`
Plan: `docs/superpowers/plans/2026-05-11-info-page.md`

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## Self-review checklist

**Spec coverage:**
- ✅ New top-level page, header nav, visible to everyone (`section: transactions` + `PAGE_SECTION` fallback) → Task 3
- ✅ Content module `INFO_SECTIONS` with all areas incl. Treasury sub-cards + glossary, plain RU → Task 1
- ✅ `InfoPage` collapsible accordion (`InfoCard`/`SubCard`) → Task 2
- ✅ i18n `nav_info` en/ru/tr (manual prose stays RU in-component) → Task 3
- ✅ Tests: content sanity (structure + core-area coverage), InfoPage render (titles + expand) → Tasks 1, 2
- ⏸ search / screenshots / en-tr manual / deep-links — deferred (Out of scope & PR body)

**Type/name consistency:** `INFO_SECTIONS` (Task 1 ↔ Task 2 import ↔ test). Section shape `{ id, title, what, related, can[], sub? }`; sub shape `{ id, title, what?, related?, can[] }` — consumed by `InfoCard`/`SubCard`. `InfoPage` default export. Nav id `"info"`, i18n key `"nav_info"` — consistent across Header & App & translations.

**Placeholder scan:** the content `can` bullets are illustrative-but-real (the implementer expands them from the spec, not fills in `…` blanks — the file as written in Task 1 Step 3 is already a valid, shippable content module that passes the tests; expansion is "improve the prose", not "fill a TODO"). The `defaultOpen` note in Task 2 Step 4 is a real one-line choice with the fix spelled out. Otherwise every step is complete and every command has expected output.

## Execution Handoff

(See the skill's handoff prompt.)
